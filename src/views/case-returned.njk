{% extends "layouts/main.njk" %}

{% from "govuk/components/panel/macro.njk" import govukPanel %}
{% from "govuk/components/button/macro.njk" import govukButton %}

{% block content %}
  {{ govukPanel({
    titleText: "Case returned",
    html: "The case has been returned to the drafter.<br><strong>Reference: " + c.id + "</strong>"
  }) }}

  <h2 class="govuk-heading-m">What happens next</h2>

  <p class="govuk-body">
    The drafter can review the comments and make changes before resubmitting.
  </p>

  <div class="govuk-button-group">
    {{ govukButton({
      text: "View case",
      href: "/cases/" + c.id
    }) }}

    {{ govukButton({
      text: "Back to cases",
      href: "/",
      classes: "govuk-button--secondary"
    }) }}
  </div>
{% endblock %}
